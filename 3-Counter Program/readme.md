### 🔢 Counter Program

A simple interactive counter application that allows users to increase, decrease, or reset a numerical value using button clicks. The counter updates dynamically on the webpage without requiring a page refresh.

**Concepts Used:**

* DOM Manipulation (`getElementById()`)
* Event Handling (`onclick`)
* Variables
* Arithmetic Operators (`++`, `--`)
* Updating HTML with `.textContent`
